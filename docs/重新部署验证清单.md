# 智联生活 · 服务器重装验证清单（每步都有通过标准）

> 用法：**一次只做一个阶段**，把该阶段的命令整段粘贴执行，对照"✅ 通过标准"确认无误后再进入下一阶段。
> 任何一步不通过：**立即停下**，把"❗失败时请贴给我"要求的内容发出来，不要继续往下做。
>
> 目标形态：阿里云 Ubuntu 22.04 单机，5 个容器（MySQL8 / Redis7 / RabbitMQ3.12 / 后端 / Nginx 前端），
> 只对外暴露 80 端口，访问 `http://<公网IP>/`。

---

## 进度总表（做完一阶段勾一个）

| 阶段 | 内容 | 通过标准（一句话） |
|---|---|---|
| 0 | 服务器彻底清空 | 无容器、无本项目数据卷 |
| 1 | 系统前置条件 | 时区/内存/Swap/Docker 全部达标 |
| 2 | 拉取正确的代码 | 关键文件齐全、`settings.xml` 在、脚本可执行 |
| 3 | 生成并校验密钥 | 无 `CHANGE_ME` 残留、compose 能解析 |
| 4 | 构建镜像 | 后端前端镜像都 Built，无 `not found` 报错 |
| 5 | 启动服务 | 5 个容器 Up，中间件 healthy |
| 6 | 三端功能验收 | 登录接口返回 `code:1`、三端账号各就其位 |
| 7 | 加固与备份（可选） | 备份可执行、密码已换 |

---

## 阶段 0 · 服务器彻底清空

> 目的：从零开始，避免上次残留的容器/数据卷（尤其是按旧 `.env.prod` 初始化的 MySQL 密码）干扰验证。

```bash
cd /opt/smartlife 2>/dev/null || cd /opt
echo "=== 清空前状态 ==="
docker ps -a --format 'table {{.Names}}\t{{.Status}}' || true
docker volume ls | grep -E 'smartlife' || true

# 停止并删除容器 + 数据卷（数据全丢，演示环境无损失）
docker compose --env-file .env.prod -f docker-compose.prod.yml down -v 2>/dev/null || true
docker rm -f smartlife-mysql smartlife-redis smartlife-rabbitmq smartlife-backend smartlife-frontend 2>/dev/null || true
docker volume rm smartlife-mysql-data smartlife-redis-data smartlife-rabbitmq-data smartlife-backend-logs 2>/dev/null || true

echo "=== 清空后状态（必须为空）==="
docker ps -a --format 'table {{.Names}}\t{{.Status}}'
docker volume ls | grep -E 'smartlife' && echo "!! 仍有残留数据卷" || echo "OK：无 smartlife 残留数据卷"
```

**✅ 通过标准**
1. 结尾输出 `OK：无 smartlife 残留数据卷`
2. `docker ps -a` 里没有 `smartlife-*` 容器（表格只有表头也算通过）

**❗失败时请贴给我**：`docker ps -a` 和 `docker volume ls` 的完整输出。

---

## 阶段 1 · 系统前置条件

```bash
echo "=== 1.1 系统版本（应为 Ubuntu 22.04）==="
lsb_release -d

echo "=== 1.2 时区（应为 CST / Asia/Shanghai）==="
timedatectl | grep -E 'Time zone|Local time'
date

echo "=== 1.3 内存与 Swap（Swap 应 >= 2.0Gi）==="
free -h

echo "=== 1.4 Docker 与 Compose（需能打印版本号）==="
docker --version
docker compose version

echo "=== 1.5 Docker 服务状态（应为 active (running)）==="
systemctl is-active docker

echo "=== 1.6 Docker 镜像加速（可选，有内容更好）==="
cat /etc/docker/daemon.json 2>/dev/null || echo "（未配置 daemon.json，不影响，只是拉镜像慢些）"
```

**✅ 通过标准**

| 检查 | 期望 |
|---|---|
| 1.1 | `Ubuntu 22.04.x LTS` |
| 1.2 | `Time zone: Asia/Shanghai (CST, +0800)` |
| 1.3 | 物理内存 ≥ 1.5Gi 可用；`Swap:` 行 ≥ `2.0Gi` |
| 1.4 | 两行都能打印版本号（无需 Docker 已在跑容器） |
| 1.5 | `active` |

**❗失败时请贴给我**：上面 6 段里**失败那一段**的输出。
（若 1.3 的 Swap 不足：`fallocate -l 2G /swapfile && chmod 600 /swapfile && mkswap /swapfile && swapon /swapfile && echo '/swapfile none swap sw 0 0' >> /etc/fstab`）

---

## 阶段 2 · 上传部署包并解压校验

> 采用**本地打包 + 上传压缩包**的方式（不依赖 GitHub，且能带上仓库里没有的文件）。
> 上传后由包内自带的 `scripts/verify-package.sh` 一次性校验"包是否完整、格式是否正确、
> 构建上下文是否健康"——也就是上次失败的两个根因（`.dockerignore` 排除 `settings.xml`、脚本缺执行位/CRLF）。

### 2A. 在你本机（Windows）打包

```powershell
cd D:\Projects\smartlife\smartlife
& .\scripts\pack-package.ps1            # 生成 tar.gz（保留可执行位，推荐）
```

期望输出末尾：

```
 已收集 185 个文件，未压缩 0.9 MB
 产物: D:\Projects\smartlife\smartlife-deploy-<时间戳>.tar.gz
 压缩后大小: 0.27 MB
 [OK] 无 node_modules / target / dist / .git / .idea
```

**✅ 通过标准**：产物路径打印出来、大小约 0.3 MB、15 个关键文件全部 `[OK]`。

### 2B. 上传到服务器（在你本机执行）

```powershell
# 把 <公网IP> 换成你的 IP（不要保留尖括号）
scp "D:\Projects\smartlife\smartlife-deploy-<时间戳>.tar.gz" root@<公网IP>:/root/
```

### 2C. 服务器上解压 + 校验（整段粘贴）

```bash
# 若已有旧目录，先挪走（避免新旧文件混在一起）
[ -d /opt/smartlife ] && mv /opt/smartlife /opt/smartlife.old.$(date +%s)

mkdir -p /opt/smartlife
cd /root
ls -lh smartlife-deploy-*.tar.gz          # 确认包已上传
tar -xzf smartlife-deploy-*.tar.gz -C /opt/smartlife
cd /opt/smartlife

echo "=== 2.1 解压后的文件数（应为 185 左右）==="
find . -type f | wc -l

echo "=== 2.2 关键文件是否都在（每行都应是 [ OK ]）==="
ls -1 docker-compose.prod.yml .env.prod.example backend/Dockerfile backend/settings.xml \
      frontend/Dockerfile frontend/nginx.conf sql/init.sql \
      scripts/verify-package.sh scripts/deploy.sh

echo "=== 2.3 运行包内自带校验（自动修复执行位/CRLF/settings.xml）==="
bash scripts/verify-package.sh --fix
```

**✅ 通过标准**

| 检查 | 期望 |
|---|---|
| 2.1 | 文件数 ≈ 185（±5 都正常） |
| 2.2 | 列出的文件全部存在，无 `No such file` |
| 2.3 | 结尾为 **`结果：PASS=... FAIL=0`** 且 **`✅ 部署包校验通过`** |

**❗失败时请贴给我**：2.1 ~ 2.3 的完整输出。

> **关于 `--fix`**：它会自动做三件事——补 `scripts/*.sh` 可执行位（Windows 的 `core.filemode=false`
> 拿不到执行位，所以这一步几乎必然需要）、把 CRLF 转成 LF、排除行残留时删掉。
> 修复后建议再跑一次不带参数的 `bash scripts/verify-package.sh`，确认 `FAIL=0`。

> **之后每个阶段的工作目录**：所有命令都要在 `/opt/smartlife` 下执行。
> 建议设个变量省事：`cd /opt/smartlife && export SL=/opt/smartlife`

---

## 阶段 3 · 生成并校验密钥

```bash
cd /opt/smartlife
cp .env.prod.example .env.prod

# 生成随机密钥并直接写入（幂等：整行替换）
sed -i "s|^MYSQL_ROOT_PASSWORD=.*|MYSQL_ROOT_PASSWORD=$(openssl rand -base64 24)|" .env.prod
sed -i "s|^REDIS_PASSWORD=.*|REDIS_PASSWORD=$(openssl rand -base64 24)|" .env.prod
sed -i "s|^RABBIT_PASSWORD=.*|RABBIT_PASSWORD=$(openssl rand -base64 24)|" .env.prod
sed -i "s|^JWT_SECRET=.*|JWT_SECRET=$(openssl rand -base64 36)|" .env.prod
chmod 600 .env.prod

echo "=== 3.1 未替换的占位符（必须无输出）==="
grep -nE '^[ \t]*[A-Za-z_][A-Za-z0-9_]*=.*CHANGE_ME' .env.prod && echo "!! 仍有占位符" || echo "OK：无占位符残留"

echo "=== 3.2 四个密钥都已写入（只显示长度，避免泄屏）==="
awk -F= '/^(MYSQL_ROOT_PASSWORD|REDIS_PASSWORD|RABBIT_PASSWORD|JWT_SECRET)=/ {printf "  %-22s 长度=%d\n", $1, length($2)}' .env.prod

echo "=== 3.3 文件权限（应为 -rw-------）==="
ls -l .env.prod

echo "=== 3.4 密码备份到文件（供你之后抄走，验证完可删）==="
grep -E '^(MYSQL_ROOT_PASSWORD|REDIS_PASSWORD|RABBIT_PASSWORD|JWT_SECRET)=' .env.prod | tee /root/smartlife-secrets.txt
chmod 600 /root/smartlife-secrets.txt

echo "=== 3.5 compose 能否正确解析（应打印出服务名，且密钥已代入）==="
docker compose --env-file .env.prod -f docker-compose.prod.yml config --services
docker compose --env-file .env.prod -f docker-compose.prod.yml config | grep -c 'CHANGE_ME' | \
  sed 's/^/  config 中 CHANGE_ME 出现次数（应为 0）：/'
```

**✅ 通过标准**
1. 3.1 输出 `OK：无占位符残留`
2. 3.2 四行都显示 `长度=32` 或 `长度=48`（base64 24/36 字节的结果），**没有长度=0**
3. 3.3 显示 `-rw-------`
4. 3.5 打印出 5 个服务名：`mysql redis rabbitmq backend frontend`；CHANGE_ME 次数为 `0`

**❗失败时请贴给我**：3.1~3.5 输出（**密钥本身请打码**，我只需要看长度和格式）。

---

## 阶段 4 · 构建镜像

> 这一步最容易出问题（上次就卡在这）。预计 5~15 分钟。

```bash
cd /opt/smartlife
docker compose --env-file .env.prod -f docker-compose.prod.yml build 2>&1 | tail -30

echo "=== 4.1 镜像是否生成 ==="
docker images | grep -E 'smartlife|REPOSITORY'

echo "=== 4.2 后端镜像里是否真的打进了 jar（应输出 app.jar）==="
docker run --rm --entrypoint sh smartlife-backend:2.0.0 -c 'ls -l /app/app.jar'

echo "=== 4.3 前端镜像里是否真的打进了静态产物（应看到 index.html + assets）==="
docker run --rm --entrypoint sh smartlife-frontend:2.0.0 -c 'ls /usr/share/nginx/html && ls /usr/share/nginx/html/assets | head -3'
```

**✅ 通过标准**
1. 构建输出末尾**没有** `ERROR` / `failed to solve` / `not found`
2. 4.1 能看到 `smartlife-backend:2.0.0` 和 `smartlife-frontend:2.0.0`
3. 4.2 输出 `app.jar`（说明容器内 Maven 构建成功）
4. 4.3 输出 `index.html`、`assets`（说明容器内 Vite 构建成功）

**❗失败时请贴给我**：构建输出的最后 40 行（`docker compose ... build 2>&1 | tail -40`）。

---

## 阶段 5 · 启动服务

```bash
cd /opt/smartlife
docker compose --env-file .env.prod -f docker-compose.prod.yml up -d

echo "=== 5.1 等待中间件与后端就绪（最多 120 秒）==="
for i in $(seq 1 24); do
  sleep 5
  RUNNING=$(docker compose --env-file .env.prod -f docker-compose.prod.yml ps --format '{{.Service}} {{.State}}' | grep -c 'running')
  READY=$(docker compose --env-file .env.prod -f docker-compose.prod.yml ps --format '{{.Service}} {{.Health}}' | grep -c 'healthy')
  echo "  第 $((i*5)) 秒：running=$RUNNING healthy=$READY"
  [ "$RUNNING" -ge 5 ] && [ "$READY" -ge 3 ] && break
done

echo "=== 5.2 最终状态 ==="
docker compose --env-file .env.prod -f docker-compose.prod.yml ps

echo "=== 5.3 端口暴露检查（关键：只有 frontend 应为 0.0.0.0）==="
docker compose --env-file .env.prod -f docker-compose.prod.yml ps --format '{{.Service}}\t{{.Ports}}'

echo "=== 5.4 后端启动日志（应含 Started SmartLifeApplication）==="
docker compose --env-file .env.prod -f docker-compose.prod.yml logs --tail=15 backend
```

**✅ 通过标准**

| 检查 | 期望 |
|---|---|
| 5.2 | 5 个容器全部 `Up`；mysql/redis/rabbitmq 为 `(healthy)`；backend 可能显示 `(health: starting)`，等 1 分钟再看 |
| 5.3 | **只有 frontend 是 `0.0.0.0:80->80/tcp`**；backend `127.0.0.1:8080`；mysql `127.0.0.1:3306`；redis `127.0.0.1:6379` |
| 5.4 | 出现 `Started SmartLifeApplication in ... seconds` 与 `索引初始化完成：布隆过滤器 + GEO 装载 10 家可展示店铺` |

**❗失败时请贴给我**：
```bash
docker compose --env-file .env.prod -f docker-compose.prod.yml logs --tail=60 backend
docker compose --env-file .env.prod -f docker-compose.prod.yml logs --tail=30 mysql
```

---

## 阶段 6 · 三端功能验收

```bash
cd /opt/smartlife
API=http://127.0.0.1/api

echo "=== 6.1 前端页面可达（应返回 HTTP 200）==="
curl -s -o /dev/null -w "  / -> %{http_code}\n" http://127.0.0.1/
curl -s -o /dev/null -w "  /login -> %{http_code}\n" http://127.0.0.1/login

echo "=== 6.2 平台管理员登录（期望 code:1 / role:3 / homePath:/admin/dashboard）==="
curl -s -X POST $API/auth/login -H 'Content-Type: application/json' \
  -d '{"phone":"13800000000","password":"admin123"}' | head -c 400; echo

echo "=== 6.3 商家登录（期望 role:2 / shopId:1 / homePath:/merchant/dashboard）==="
curl -s -X POST $API/auth/login -H 'Content-Type: application/json' \
  -d '{"phone":"13700000001","password":"merchant123"}' | head -c 400; echo

echo "=== 6.4 用户登录（期望 role:1 / homePath:/user/home）==="
curl -s -X POST $API/auth/login -H 'Content-Type: application/json' \
  -d '{"phone":"13900000001","password":"123456"}' | head -c 400; echo

echo "=== 6.5 用户端店铺列表（期望 code:1 且 10 家店）==="
UTOK=$(curl -s -X POST $API/auth/login -H 'Content-Type: application/json' \
  -d '{"phone":"13900000001","password":"123456"}' | sed -n 's/.*"token":"\([^"]*\)".*/\1/p')
curl -s "$API/shop/list?page=1&size=20" -H "Authorization: Bearer $UTOK" | head -c 200; echo
echo "  店铺数量：$(curl -s "$API/shop/list?page=1&size=20" -H "Authorization: Bearer $UTOK" | grep -o '"id"' | wc -l)"

echo "=== 6.6 越权拦截（用户 token 调管理端，期望 code:403）==="
curl -s "$API/admin/stats/overview" -H "Authorization: Bearer $UTOK" | head -c 200; echo

echo "=== 6.7 数据库三端数据（期望 role 1:9 / 2:11 / 3:1）==="
docker compose --env-file .env.prod -f docker-compose.prod.yml exec -T mysql \
  mysql -uroot -p"$(grep '^MYSQL_ROOT_PASSWORD=' .env.prod | cut -d= -f2)" -N -e \
  "USE smartlife; SELECT CONCAT('role=',role,' 数量=',COUNT(*)) FROM tb_user GROUP BY role; \
   SELECT CONCAT('店铺数=',COUNT(*)) FROM tb_shop; \
   SELECT CONCAT('待审核申请=',COUNT(*)) FROM tb_merchant_apply WHERE status=0;" 2>/dev/null
```

**✅ 通过标准**

| 检查 | 期望 |
|---|---|
| 6.1 | 两个都是 `200` |
| 6.2 | `"code":1` 且含 `"role":3`、`"homePath":"/admin/dashboard"` |
| 6.3 | `"code":1` 且含 `"role":2`、`"shopId":1` |
| 6.4 | `"code":1` 且含 `"role":1`、`"homePath":"/user/home"` |
| 6.5 | `"code":1`，店铺数量为 `10` |
| 6.6 | `"code":403` |
| 6.7 | 三行输出：`role=1 数量=9`、`role=2 数量=11`、`role=3 数量=1`、`店铺数=10`、`待审核申请=1` |

**浏览器再确认一次**（把 IP 换成你的公网 IP，安全组需放行 80）：

| 端 | 地址 | 账号 | 该看到什么 |
|---|---|---|---|
| 用户端 | `http://<IP>/` | `13900000001` / `123456` | 自动跳到 `/user/home`，有 10 家店铺 |
| 商家端 | `http://<IP>/merchant` | `13700000001` / `merchant123` | 左侧显示「蜀香居川菜馆 · 营业中」 |
| 管理端 | `http://<IP>/admin` | `13800000000` / `admin123` | 平台看板 + 「入驻审核」有 1 条待审 |

**❗失败时请贴给我**：6.1~6.7 的完整输出 + 对应端浏览器页面的报错（F12 Console/Network）。

---

## 阶段 7 · 加固与备份（可选，验收通过后再做）

> ⚠️ **重要前提**：`MYSQL_ROOT_PASSWORD` 只在数据卷**第一次初始化**时生效。
> 阶段 3 已经用随机密码初始化过了，所以现在就不要再改 `.env.prod` 里的 MySQL 密码——
> 改了必须连数据卷一起删掉重建。本项目当前密码已经是随机的，**不需要再改**。

```bash
cd /opt/smartlife

echo "=== 7.1 备份脚本可用 ==="
./scripts/backup-db.sh
ls -lh backup/

echo "=== 7.2 挂每日自动备份（凌晨 3 点）==="
(crontab -l 2>/dev/null; echo '0 3 * * * cd /opt/smartlife && ./scripts/backup-db.sh >> /var/log/smartlife-backup.log 2>&1') | crontab -
crontab -l

echo "=== 7.3 便捷命令 sl（任意目录可用）==="
cat > /usr/local/bin/sl <<'EOF'
#!/usr/bin/env bash
cd /opt/smartlife || { echo "项目目录 /opt/smartlife 不存在"; exit 1; }
exec docker compose --env-file .env.prod -f docker-compose.prod.yml "$@"
EOF
chmod +x /usr/local/bin/sl
sl ps
```

**✅ 通过标准**
1. 7.1 在 `backup/` 下生成 `smartlife_*.sql.gz` 且大小 > 0
2. 7.2 `crontab -l` 能看到那一行
3. 7.3 `sl ps` 正常列出 5 个容器

---

## 失败排查速查（先自查，再贴给我）

| 症状 | 先看这里 |
|---|---|
| `couldn't find env file: /root/.env.prod` | 工作目录不对，先 `cd /opt/smartlife` |
| `./scripts/deploy.sh: Permission denied` | `chmod +x scripts/*.sh` 或用 `bash scripts/deploy.sh` |
| `"/settings.xml": not found` | 阶段 2.3：从 `backend/.dockerignore` 删掉 `settings.xml` 行 |
| `Killed` / 构建被打断 | 内存不足：确认 Swap ≥ 2G（阶段 1.3） |
| 登录报"服务器繁忙" | `sl logs --tail=60 backend` 找 ERROR；多为 MySQL 未就绪或密码不一致 |
| 浏览器打不开 | 先 `curl -I http://127.0.0.1/`：本机通就是**安全组没放行 80** |
| 改了 `.env.prod` 密码后连不上库 | 数据卷里是旧密码。要么改回，要么 `down -v` 后重新初始化 |
